# リリースメモ

## 概要

人事評価アプリの初回運用向け改善を反映しました。  
ユーザー管理、年度管理、評価画面、AI支援機能を中心に見直しています。

## 主な変更点

### 1. ユーザー管理

- ユーザー情報の正本を `users.csv` に統一
- CSV更新内容をアプリへ同期
- CSVに存在しないユーザーはDBから物理削除
- CSVの文字コードは UTF-8 / Shift-JIS(cp932) に対応

### 2. 初回ログイン時のパスワード変更

- 初回ログイン時にパスワード変更を必須化
- 変更後のパスワードは `users.csv` にも自動反映
- `users.csv` の `要パスワード変更` 列で制御

### 3. 年度管理

- 年度情報を復活
- 上長専用の `年度設定` 画面から現在年度を変更可能
- 各入力画面は設定された現在年度のデータを表示・保存

### 4. 入力画面の改善

- 上長も `個人目標入力` と `達成状況入力` を利用可能
- 個人目標入力:
  部署目標を見ながら4項目を横並びで入力可能
- 達成状況入力:
  `部署目標 / 個人目標 / 達成状況` の3カラム4行表示
- 評価入力:
  `部署目標 / 個人目標 / 達成状況 / AI評価 / 上長最終評価 / 点数`
  の6カラム4行表示
- 評価対象は同一部署のユーザー全員
  自分自身も選択可能
- 評価対象の切替時は、入力途中の内容を保存してから画面切替

### 5. AI評価支援

- 評価画面に4項目ごとの `AI評価生成` ボタンを追加
- AI評価は部署目標・個人目標・達成状況をもとに生成
- `年度設定` 画面で以下を変更可能
  - AIエンドポイント
  - AIモデル名
  - AI評価プロンプト

## CSV仕様

現在の `users.csv` ヘッダ:

```csv
id,名前,部署,上長フラグ,パスワード,要パスワード変更
```

## 注意事項

- AI機能を使うには、設定した AI エンドポイントにアプリ実行環境から到達できる必要があります
- AIモデル名は Vertex AI 側で利用可能なモデル名を設定してください
- 年度を変更すると、各画面はその年度のデータを対象に表示・保存します

## 反映済み内容

- `master` ブランチへ反映済み
